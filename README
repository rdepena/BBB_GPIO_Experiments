bare bones GPIO library for the BeagleBone implemented using sysfs.

Three Steps fs based BeagleBone GPIO:

Digital IO:
1) Export the Pin
2) Set the direction
3) Write/Read the value.

Analog IO:
-- only AnalogRead supported.
1) Load the ADC capemgr
2) Read the value


--todo:
add analog write support
