GPIO Experiments on the BeagleBone Black.

Super bare bones implementation using sysfs.

Three Steps fs based BeagleBone GPIO:

1) Export the Pin
2) Set the direction
3) Write/Read the value.

--Implementation so far

digital read/write on gpio pins.

--todo:
error checking handling
analog read/Write
make install/uninstall tasks.
